# Skill — hyprland-plugin

Expert skill for **Hyprland native C++ plugins**.

Covers PluginAPI lifecycle, Event::bus, dispatchers V2, config rules, hooks, hyprpm, and failure modes.

**Triggers:** plugin scaffolding, `PLUGIN_INIT`, load failures, Event bus vs hooks, reviewing a `.so` plugin.

**Companion docs in repo:** `docs/HYPRLAND-PLUGIN-SYSTEM.md`
