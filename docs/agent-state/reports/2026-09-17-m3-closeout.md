# M3 closeout — USER.md vs real behavior + config reload documented

**Date:** 2026-09-17
**Branch:** `docs/m3-closeout` (docs-only; nothing committed)
**Scope:** ROADMAP M3 deliverables "Config reload behaviour documented and tested" and "USER.md validated against real behaviour" (documentation legs).

## What changed

| File | Change | SPEC / evidence |
|------|--------|-----------------|
| `docs/USER.md` | New section **Config reload**: values read at load; `hyprctl reload` → `config.reloaded`; `debounce_ms` applies to post-reload MRU updates; other keys apply to the **next** session; active session untouched | REQ-CFG-002/003, REQ-S-009; `mru_plugin.cpp:190-199` listener; `mru_plugin.cpp:204-223` build_state; T-CFG-02 |
| `docs/USER.md` | `mru:status` row + note: `hyprctl` prints bare `ok` on 0.56.x, payload only visible on failure; suggest `hyprctl activewindow -j` for state checks | COMPAT.md §IPC note (0.56.2); `mru_plugin.cpp:111-114` (payload in success result's `error`) |
| `docs/USER.md` | FAQ scratchpad: hidden scratchpad excluded from **every** scope; shown scratchpad follows the scope. Old advice ("use workspace/visible to limit") misleading — hidden is excluded everywhere | REQ-SC-002a; live nest §2–3 (shown: in all 5; hidden: 0/16 in all 5) |
| `docs/USER.md` | Manual checklist: scope + scratchpad items linked to live nest report; config-reload item linked to T-CFG-02 (unit) | `docs/agent-state/reports/2026-09-17-m3-s3-nest-smoke.md` §2–5; `tests/domain/test_session_controller.cpp:588-619` |
| `docs/FAILURE-MODES.md` | FM-05 tests cell `config reload test M3` (open) → `T-CFG-02`; also cites REQ-CFG-003/REQ-S-009 | T-CFG-02 (reload → next session only) |

No changes to `docs/REQ-TRACE.md`: REQ-CFG-001..004 rows (lines 70–73) and T-CFG-* index (lines 131–136) are already mapped to closed tests; none are marked open/missing. No new tests invented.

## Scope table verification (USER.md "Scopes")

All five rows match live behavior in `2026-09-17-m3-s3-nest-smoke.md` §4–5:

| Scope | USER.md wording | Live evidence |
|-------|-----------------|---------------|
| `global` | All mapped windows | step 4: {A,B,C,D} = 4/4 + pre-load windows §8 |
| `monitor` | Current monitor | step 4: mon0 = {A,B,D} only |
| `workspace` | Current workspace | step 4: ws1 = {A,B} only |
| `visible` | Currently visible workspaces | step 4: shown = {A,B,C} |
| `app` | Same class as active | step 5: foot anchor = {A,C} byte-exact, `Foot` excluded |

Key not yet in the table: with **no focused window** at snapshot time, `monitor`/`workspace`/`app` behave as `global` (SPEC §2, ADR-016; live step 6). This is a real, verified behavior gap in USER.md — not fixed here (needs a sentence in the Scopes section; see gaps).

## Explicit gaps / notes

1. **Focus-less degrade — DONE (orchestrator follow-up).** Added one sentence to USER.md "Scopes": with no focused window `monitor`/`workspace`/`app` behave as `global`; `visible` still filters; unknown scope token → `unknown scope token` error, no session starts (SPEC §2, ADR-016, live nest step 6).
2. **`ui` backend not swapped at runtime yet.** `build_state` hardwires `NullUI`; the reload listener re-reads `ui` into config but no backend swap exists before M4/M5. USER.md reload wording follows SPEC REQ-CFG-003 ("ui → next session") and the existing "border/external fall back to null" caveat; a live ui-switch proof is M4 territory.
3. **Config reload live nest run — DONE.** `docs/agent-state/reports/2026-09-17-m3-reload-smoke.md`: PASS 5 / FAIL 0 / NOT-RUNNABLE 1. `hyprctl reload` flipped `getoption` (global→workspace, debounce 400→1000); the **active** session after reload applied the pre-reload global selection (snapshot not rewritten, REQ-S-009); a **new** session used `workspace` (REQ-CFG-002). `ui` backend swap remains NOT-RUNNABLE by design (only `null` exists; M4/M5) — REQ-CFG-003.
4. **`mru:status` payload unobservable via `hyprctl` on 0.56.2** — known IPC limitation (COMPAT note), now reflected in USER.md; not a plugin defect.
5. **PROGRESS.md M3 rows ready to tick** ("Config reload behavior documented + tested", "USER.md validated") once this branch is reviewed/merged by the orchestrator.